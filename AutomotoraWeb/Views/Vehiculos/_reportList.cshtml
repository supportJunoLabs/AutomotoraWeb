@using AutomotoraWeb.Controllers.General;
@using AutomotoraWeb.Controllers.Sales;
@using AutomotoraWeb.Models;

@{
    InfoReporte ir = new InfoReporte();
    ir.AccionReporte = BaseController.REPORT_PARTIAL;
    ir.Controlador = VehiculosController.CONTROLLER;
    ir.AccionExportacion = BaseController.REPORT_EXPORT;
    ir.TieneIdParametros= true;

    ViewBag.InfoReporte = ir;
    }

@Html.Partial("_generalReport")

   @* // The following settings are required for a Report Viewer. 
    settings.Name = "documentViewer";
    settings@Html.DevExpress().DocumentViewer(settings =>{
.Report = (DevExpress.XtraReports.UI.XtraReport)ViewData["Report"];
    // Callback and export route values specify corresponding controllers and their actions. 
    // These settings are required as well. 
    settings.CallbackRouteValues = new { Controller = VehiculosController.CONTROLLER, Action = BaseController.REPORT_PARTIAL, idParametros = Model.idParametros };
    settings.ExportRouteValues = new { Controller = VehiculosController.CONTROLLER, Action = BaseController.REPORT_EXPORT, idParametros = Model.idParametros };
}).GetHtml()*@
