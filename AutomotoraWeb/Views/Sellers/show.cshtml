@model List<AutomotoraWeb.Models.SellerModel>

@{
    ViewBag.Title = "Vendedores";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row-fluid">
    <div class="btn-toolbar">
        <div class="btn-group">
            @(Html.Bootstrap().ActionLinkButton("Agregar Vendedor", "Create").AppendIcon(TwitterBootstrapMVC.Icons.plus, true).Style(TwitterBootstrap2.ButtonStyle.Primary).Title("Agregar Vendedor"))
        )
        </div>
        @*<div class="btn-group">
            <a class="btn btn-primary2-mf" title="Imprimir listado" href='@Url.Action("Create", "Vendedores")'>
                Imprimir</a>
        </div>*@
         <div class="btn-group btn-primary2-mf">
                <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">Exportar <span class="caret">
                </span></a>
                <ul class="dropdown-menu">
                    <li><a href='@Url.Action("ExportarExcel")'>Exportar a Excel</a></li>
                    <li><a href='@Url.Action("ExportarPDF")'>Exportar a PDF</a></li>
                </ul>
        </div>
    </div>
       
    <div id="sellersGrid">
        @Html.Partial("_listSellers", Model)
    </div>
</div>

