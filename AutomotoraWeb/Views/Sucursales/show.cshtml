@model List<DLL_Backend.Sucursal>

@{
    ViewBag.Title = "Sucursales";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row-fluid">
    <div class="btn-toolbar">
        <div class="btn-group">
            @(Html.Bootstrap().ActionLinkButton("Agregar Sucursal", "Create").AppendIcon(TwitterBootstrapMVC.Icons.plus, true).Style(TwitterBootstrap2.ButtonStyle.Primary).Title("Agregar Sucursal"))
        )
        </div>
        @*<div class="btn-group">
            <a class="btn btn-primary2-mf" title="Imprimir listado" href='@Url.Action("Create", "Vendedores")'>
                Imprimir</a>
        </div>*@
         <div class="btn-group btn-primary2-mf">
                <a class="btn dropdown-toggle" data-toggle="dropdown" href="#">Exportar <span class="caret">
                </span></a>
                <ul class="dropdown-menu">
                    <li><a href='@Url.Action("ExportarExcel")'>Exportar a Excel</a></li>
                    <li><a href='@Url.Action("ExportarPDF")'>Exportar a PDF</a></li>
                </ul>
        </div>
    </div>
       
    <div id="sucursalessGrid">
        @Html.Partial("_listSucursales", Model)
    </div>
</div>

