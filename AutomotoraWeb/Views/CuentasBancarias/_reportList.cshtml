@using AutomotoraWeb.Controllers.Bank.Maintenance;
@using AutomotoraWeb.Controllers.General;


@Html.DevExpress().DocumentViewer(settings =>{
    // The following settings are required for a Report Viewer. 
    settings.Name = "documentViewer";
    settings.Report = (DXReportCuentasBancarias)ViewData["Report"];
    // Callback and export route values specify corresponding controllers and their actions. 
    // These settings are required as well. 
    settings.CallbackRouteValues = new { Controller = CuentasBancariasController.CONTROLLER, Action = BaseController.REPORT_PARTIAL };
    settings.ExportRouteValues = new { Controller = CuentasBancariasController.CONTROLLER, Action = BaseController.REPORT_EXPORT };
}).GetHtml()
