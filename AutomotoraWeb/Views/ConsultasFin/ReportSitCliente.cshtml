@model DLL_Backend.Cliente
@using DLL_Backend;
@using AutomotoraWeb.Controllers.General;

@{ 
    ViewBag.Title = "Situacion Cliente";
}

<div class="btn-toolbar">
    @(Html.Bootstrap().ActionLinkButton("Volver", "ListSitCliente").Class("btn btn-default").RouteValues(new{id=Model.Codigo}))
</div>
<div class="clear-both"></div>


@Html.Partial("_reportSitCliente", Model.Codigo)