@using AutomotoraWeb.Controllers.Financing;
@{ 
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Recibo Entrada / Salida Caja";
}
@section HeadResources{
    @Html.Partial("_devExpressReporte")
}

<span class="validation-summary-errors">@ViewBag.ErrorMessage</span>

<div class="btn-toolbar">
    @(Html.Bootstrap().ActionLinkButton("Volver", FinancingController.INDEX, FinancingController.BCONTROLLER).Class("btn btn-default"))
</div>
<div class="clear-both"></div>

@{ if (ViewData["idParametros"]==null || (int)ViewData["idParametros"]==0) { return; }}

@Html.Partial("_reciboCaja")